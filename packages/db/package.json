{
  "name": "@repo/db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "generate": "npx prisma generate",
    "reset": "pnpm prisma migrate reset",
    "migrate": "pnpm prisma migrate dev",
    "db:deploy": "pnpm prisma migrate deploy && bun run generate",
    "studio": "npx prisma studio"
  },
  "exports": {
    "./client": "./index.ts"
  },
  "devDependencies": {
    "prisma": "^6.18.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/client": "^6.18.0",

    "@repo/typescript-config": "workspace:*"
  }
}
