export function callModel_Api(opts:{

    model:string,
    prompt:string,
    api_key:string
}){
    const { model, prompt, api_key } = opts;
    if(model=="gpt-4"){

    }
    if(model == "gemini"){

        
    }


    
}